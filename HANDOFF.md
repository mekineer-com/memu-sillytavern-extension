
- **2026-06-11 — Vale (memu-extension scan + cleanup — repo 4 CLOSED)** — Sonnet scan found the extension nearly clean: all ST 1.18 event hooks/DOM selectors live, all plugin/server contract fields matched, memorize polling + undo flow fully wired. Removed in `af561cb` (−148): utils/utils.ts (estimateTokenUsage), ui/status.tsx (4 unused icons), log.ts status()/onceInfo(), network.ts pingPlugin(), PluginPing.module ghost field, 'all' MemuStep entry; added missing `tail?: boolean` to MemuSummary (was written/read untyped). Scanner's retry-label "bug" rejected — one-retry flow is by design (`277c1c3`). Build clean. Remaining: repo 5 hermes-agent.
